// Re-export types from website-builder so public-website uses the same types
export type {
  AdmissionLeadRequest,
  GalleryItem,
  PublicWebsiteData,
  WebsiteConfig,
  WebsiteSection,
} from '../website-builder/types'
